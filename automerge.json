{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "packageRules": [
    {
      "matchUpdateTypes": [
        "{{arg0}}"
      ],
      "labels": [
        "dependencies",
        "renovate-{{arg0}}"
      ],
      "automerge": true
    }
  ]
}